import { NextPage } from "next";

const ConnectionsPage: NextPage = () => {
    return (
        <div>
            DB Connections page routing works!
        </div>
    );
};

export default ConnectionsPage
